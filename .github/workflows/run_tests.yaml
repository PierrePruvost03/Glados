name: run tests

on:
  pull_request:
  push:
    branches:
      - main

env:
  STACK_ROOT: /tmp/stack-root

jobs:
  run_tests:
    runs-on: ubuntu-latest
    container:
      image: epitechcontent/epitest-docker
    steps:
      - uses: actions/checkout@v4
      - name: run unit tests
        run: make tests_run
      - name: run functional tests
        run: sudo apt-get install python-venv && ./functionnal_tests/install_requirements.sh && source .venv/bin/activate && python3 functionnal_tests/run.py
