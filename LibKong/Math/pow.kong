Funk pow(Int a, Int b) -> Int {
    Int result = 1;
    If (b `==` 0) {
        Return result;
    } Else If (b `<` 0) {
        Return 0;
    } Else {
        For (Int i = 0; i `<` b; i = i + 1) {
            result = result * a;
        }
        Return result;
    }
}
