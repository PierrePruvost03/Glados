Inklude isAlpha (isAlpha)
Inklude isDigit (isDigit)

Funk isAlphaNum(Khar k) -> Bool {
    If (isAlpha(k)) {
        Return true;
    }
    If (isDigit(k)) {
        Return true;
    }
    Return false;
}