Inklude isLower (isLower)

Funk toUpper(Khar k) -> Khar {
    Int new_k = 0;
    Int transition = 0;
    If (isLower(k)) {
        new_k = Kast(Int, k);
        transition = Kast(Int, 'A') - Kast(Int, 'a');
        new_k = new_k + transition;
        Return Kast(Khar, new_k);
    }
    Return k;
}
